#!/bin/bash

if [ "$#" -ne 3 ]; then
    echo "usage: <renderer_check> <caselist> <output>"
    exit 1
fi

renderer_check=$1
caselist=$2
output=$3
skips=$MESA_ROOT_DIR/.gitlab-ci/all-skips.txt

run_deqp "$renderer_check" "vk" "$caselist" "$skips" "$output"
